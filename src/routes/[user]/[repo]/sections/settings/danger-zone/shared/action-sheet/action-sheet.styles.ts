import styled from 'styled-components';

import { ActionSheet as BaseActionSheet } from '../../../shared';

const ActionSheet = styled(BaseActionSheet)`
  background-color: transparent;
  border: none;
`;

const Styled = { ActionSheet };

export default Styled;
